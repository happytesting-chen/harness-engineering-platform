# AI Stack — <PROJECT_NAME>

> Rename to `ai-stack.md` and fill. Records the AI-development choices for this agent
> and *why*. Link it from CLAUDE.md's Domain Context.

## Agent framework

- **Choice:** <e.g. LangChain / LangGraph / Strands / LlamaIndex / custom loop>
- **Why:** <what this project needs that drove the choice — tool orchestration,
  multi-agent, streaming, graph control flow, minimal deps, …>
- **Version pin:** <exact version — see supply-chain guidance in Security-kit/SECURITY.md §5>
- **Where the four gates attach in it:** <the framework owns the loop; name the seam where `RuntimeDispatcher.execute()` replaces its tool caller, and where `screen_input()` sits at the request boundary — see `Security-kit/SECURITY.md` S1.6 and record the wiring in `Context/deployment.md`>

## Model

- **Provider / model:** <e.g. Claude Opus 4.x, via Bedrock / API>
- **Why this tier:** <reasoning depth vs cost/latency for this task>
- **Fallback / secondary:** <cheaper model for classification/validation sub-tasks, if any>
- **Pinned:** <do not silently switch models mid-project — record the pin>

## Tools the agent can call

| Tool | Purpose | In `governance/mcp-allowlist.json`? | Egress? | Reached only via the dispatcher? |
|------|---------|------------------------------------|---------|----------------------------------|
| <tool> | <what it does> | yes/gated_until phase-XX | none / host | yes / no — if no, say why |

> Every tool the agent uses must be in the allowlist. New tools need human approval, and
> `governance/mcp-allowlist.json` is a protected path — registering one is a human edit.
> An unregistered tool denies with `<name> not in allowlist`; that is gate ② working.
> The last column is the one that decides whether the deployed agent is actually gated: a
> tool a code path can call directly, without going through `RuntimeDispatcher.execute()`,
> is ungated no matter what this table says (`SEC-RUNTIME-GAP-001`).

## Key libraries / dependencies

- <lib> — <purpose> — <exact version pin>

## Open questions / decisions pending

- <anything not yet decided>
