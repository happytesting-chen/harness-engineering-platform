# Deployment Target — <PROJECT_NAME>

> Rename to `deployment.md` and fill. Records where and how this agent runs. Deployment
> choices drive egress policy, secret handling, and several security controls — keep this
> consistent with `security/shared/mcp-allowlist.json`.

## Target

- **Environment:** <on-prem / private cloud / public cloud (AWS/GCP/Azure) / local-only>
- **Why:** <data residency, latency, compliance, cost, existing infra…>
- **Runtime:** <container / serverless / VM / local process; OS/arch if it matters>

## Data residency & boundaries

- **Where does data live:** <region / on-prem store>
- **What may leave the environment:** <maps to `egress_hosts` in `security/shared/mcp-allowlist.json`>
- **Sensitive data classes handled:** <PII / secrets / regulated data — see `security/shared/SECURITY.md` §3>

## Secrets & identity

- **Secret source:** <env vars / secret manager — never hardcoded (secret-block hook enforces)>
- **Agent identity / auth:** <how the agent authenticates to tools/services; short-lived creds?>

## Egress

- **Allowed outbound hosts:** <list — must match `security/shared/mcp-allowlist.json` egress_hosts>
- **Default:** deny (the permission gate blocks unlisted hosts)
- **Matching is exact:** a subdomain is not covered unless you list it as `*.example.com`

## Runtime enforcement wiring

The hooks in `.claude/settings.json` fire in an IDE session only. A deployed agent gets
**none** of that enforcement unless it routes through the in-process modules. Record what
this deployment actually does:

- **Input screen (data rule):** <where is `runtime_screen.screen_input()` called before content enters the model? List each entry point — user input, fetch results, etc. / not wired>
- **Action gate (action rule):** <where is `RuntimeDispatcher.execute()` constructed, and can any tool be reached without it? / not wired>
- **Tools registered:** <must be present in `security/shared/mcp-allowlist.json`; an unregistered tool is denied at runtime>
- **Output screen:** <where is `screen_result()` called before model output reaches the user or a downstream system? / not wired>
- **If not wired:** <state the accepted residual and why — `SEC-RUNTIME-GAP-001`>

Nothing in the repo verifies this section. It is an attestation, and a wrong one here is
indistinguishable from a right one — which is exactly why it is written down.

## Operational

- **Observability sink:** <where observability/audit.log ships in production>
- **Rollback / kill switch:** <how to stop the agent safely>
