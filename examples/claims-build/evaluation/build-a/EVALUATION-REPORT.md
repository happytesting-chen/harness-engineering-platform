# Build A — Phase-03 Evaluation Report

> **Status:** PROPOSED for human review. Narrative evidence for phase-03, companion
> to the machine-generated `SNAPSHOT.md` (metrics) in this directory.
> **Evaluated:** 2026-08-04 (UTC 2026-08-04T03:42Z). **Evaluator session:** phase-03.
> This is an evaluation record, not a new feature. No implementation code changed.

## What this report attests

Build A is the deterministic claims core: `validate → normalize → route → one
minimal fail-closed write`, governed by the three-gate permission model. Every
claim below is backed by a command run this session and its observed result.
`SNAPSHOT.md` (generated by `evaluation/eval.py`) carries the headline metrics;
this file carries the domain-specific correctness/traceability detail.

## 1. Verification (rerun unchanged, exit codes recorded)

| Command | Exit | Result |
|---------|------|--------|
| `./init.sh` | 0 | PASS — placeholders, freshness, fixtures 7/7, E2E 3/3, Security-kit integrity, hook-path integrity, eval reference 100%, fresh-session 5/5 |
| `python3 -m pytest tests claims/tests -q` | 0 | 38 passed (19 retained core, unchanged + 19 claims incl. runner) |

Retained core enforcement tests were **not** modified to obtain the pass.

## 2. Correctness — all three terminal outcomes exercised

Real entrypoint `python3 -m claims.runner <fixture>` (decide-only), over the five
committed synthetic `SYN-*` fixtures:

| Fixture | claim_id | Outcome | Reason |
|---------|----------|---------|--------|
| 001-approved | SYN-CLAIM-001 | APPROVED | EXACT_COVERAGE |
| 002-rejected | SYN-CLAIM-002 | REJECTED | COVERAGE_SHORTFALL |
| 003-review-untrusted | SYN-CLAIM-003 | PENDING_REVIEW | UNTRUSTED |
| 004-review-malformed | SYN-CLAIM-004 | PENDING_REVIEW | MALFORMED_AMOUNT |
| 005-review-safety | SYN-CLAIM-005 | PENDING_REVIEW | SAFETY_FLAGGED |

APPROVED / REJECTED / PENDING_REVIEW all reached. Routing rule confirmed:
`covered < submitted → REJECTED`, `covered > submitted → PENDING_REVIEW`,
`covered == submitted → APPROVED`; trust/safety/incompleteness gates precede
monetary math (all defects → PENDING_REVIEW, never REJECTED).

Also cross-checked with the generic evaluator over the same fixtures
(`claims.pipeline.decide`, oracle = `expected.outcome`): **accuracy 100% (5/5),
reproducibility 100% (5/5)** — proving `evaluation/eval.py` is task-agnostic, not
gate-only.

## 3. Traceability — each result matches its fixture oracle

The `expected` block in each fixture is an **oracle** (asserted in tests, never
fed to `decide()`). Cross-checked actual-vs-oracle this session: **5/5 OK**.
Independence proven by `test_expected_block_does_not_influence_decision` (a
tampered oracle does not change the APPROVED outcome).

Gate ↔ tool binding: `claims_runner` is a real, invokable entrypoint
(`claims/runner.py`), not just an allowlist token. `python3 demo/claims_demo.py`
observed:

```
✓ allow    bash(echo 'intake received')
⛔ DENIED   claims_runner(...)            # phase-01 active-but-not-passing
↻ phase-01 → passing (human sign-off); phase-02 → active
✓ allow    claims_runner(...)            # now runs the REAL runner
{"claim_id":"SYN-CLAIM-001","outcome":"APPROVED","reason_code":"EXACT_COVERAGE"}
```

## 4. Local reproducibility

- **Byte-identical results across runs:** writing SYN-CLAIM-001 into two fresh
  boundaries yields identical `sha256 = e738d75f…4fc354`.
- **Fail-closed writer:** a duplicate write into an occupied boundary is refused
  (`exit=2`, `duplicate write refused`), leaving exactly 1 file.
- **Determinism** additionally proven in `claims/tests/test_determinism.py`.

## 5. Isolation / no prohibited effect

- **stdlib-only guard:** no `requests/urllib/http/socket/boto3/openai/anthropic/subprocess`
  imports in `claims/` (excluding tests). Observed: none found.
- No LLM/provider, network, cloud, credentials, email, or deployment. Writes are
  limited to approved local results (via `--out`) and this evaluation evidence.

## Scope boundaries honored in phase-03

- No new features; no implementation code added or changed.
- Only evaluation evidence + the generic `evaluation/` primitive were written.
- Agent did **not** self-transition — phase-03 remains `active`, awaiting sign-off.

## Known backlog (recorded, non-blocking — do not fix under phase-03)

- **init.sh line ~264 cosmetic bug:** Q5 fresh-session check prints
  `[: integer expression expected` (a count returns two lines). Non-fatal; Q5 and
  the overall run still PASS (exit 0). Maintenance fix, mirror to canonical template.
- Security-kit deliberately omitted this phase (A/B deferred to the security phase).
- Fix #2 (content-trust matrix redundancy) deferred to the security phase.
- Template drift (`security/` → `Security-kit/`) and unfilled Kiro `{{…}}` stubs —
  cosmetic, outside init.sh's gate scope.

## Requested action

Human review of `SNAPSHOT.md` (metrics) + this report. On approval, record phase-03
sign-off in `feature_list.json` and proceed to the security phase (install
Security-kit via `install.sh --no-security` on a copy for the A/B comparison).
